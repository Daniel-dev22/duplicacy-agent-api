# Ansible-deploy runtime artifacts (root-owned, per-host secrets)
bearer-token
app
docker-compose.yml
docker-compose.yml.*~

# Build output
/build/
# `go build` in the repo root emits a binary named after the module
/duplicacy-agent-api

# State
state/
known_hosts
